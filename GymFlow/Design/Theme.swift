import SwiftUI

struct Theme {
    static let background = Color(hex: "#111113")
    static let cardBackground = Color(hex: "#1C1C1E").opacity(0.82)
    static let amber = Color(hex: "#E8A135")
    static let text = Color(hex: "#F5F4F2")
    static let textSecondary = Color(hex: "#F5F4F2").opacity(0.55)
    static let green = Color(hex: "#32D74B")
    static let blue = Color(hex: "#4A9EFF")
    static let red = Color(hex: "#FF453A")
    static let purple = Color(hex: "#BF5AF2")
    static let orange = Color(hex: "#FF9F0A")
    static let teal = Color(hex: "#5AC8FA")
    static let pink = Color(hex: "#FF6482")
    
    static let palette = [
        "#4A9EFF", "#32D74B", "#E8A135", "#FF453A",
        "#BF5AF2", "#FF9F0A", "#5AC8FA", "#FF6482"
    ]
    
    static let icons = ["🏋️","💪","🏃","🚴","🤸","🧘","🥊","🔥","⚡","🎯","🦁","🐆"]
}
